//let score="33ab"

//console.log(typeof score);//method-1
//console.log(typeof (score));//method-2
//if 33 is written in double quotes then the value will be in strings(datatype)

//let valueInNumber = Number(score)//the score will convert into number
//console.log(typeof valueInNumber);
//console.log(valueInNumber);//Nan(Not a number)
//if score is null then output value will be 0
//if score is undefined then output is undefined
//if score is a boolean value then True is 1 and false is 0
//if there is a string which cant't be converted into a number for example your name then if we try to convert it into a name then output value will be Nan

//NOTES:-
//"33" => 33
//"33abc" => NaN
//true => 1; false => 0;

//let isLoggedIn = 1
//let booleansIsLoggedIn = Boolean(isLoggedIn)
//console.log(booleansIsLoggedIn);

let isLoggedIn = ""//empty string
let booleansIsLoggedIn = Boolean(isLoggedIn)
console.log(booleansIsLoggedIn);
//NOTES:-
//1 => true
//0 => false
//"" => false
//"Ayushi" => true

let someNumber = 33
let stringNumber = String(someNumber)
console.log(stringNumber)
console.log(typeof stringNumber)
//browser gives us string values